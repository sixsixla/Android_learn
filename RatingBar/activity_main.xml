<?xml version="1.0" encoding="utf-8"?>
<!--程序采用新型布局。子元素控件排列方式为垂直排列，整个布局高度同父元素相同且宽度同父元素相同-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <!--显示ratingBarStyle进度的文本视图。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    <TextView android:id="@+id/textviewDefault"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="默认评分条"/>
    <!--默认评分条。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    ...
    <!--显示ratingBarStyleSmall进度的文本视图。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    <TextView android:id="@+id/textviewSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="Small评分条"/>
    <!--评分条ratingBarStyleSmall。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    <RatingBar android:id="@+id/ratingBarStyleSmall"
        style="?android:attr/ratingBarStyleSmall"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:numStars="6"
        android:layout_gravity="center_vertical" />
    <!--显示ratingBarStyleIndicator进度的文本视图。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    <TextView android:id="@+id/textviewIndicator"
        android:text="Indicator评分条"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"/>
    <!--评分条ratingBarStyleSmall。该控件宽度随组件本身的内容所调整，高度随组件本身的内容所调整-->
    <RatingBar
        android:id="@+id/ratingBarStyleIndicator"
        style="?android:attr/ratingBarStyleIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:numStars="6"
        android:rating="3.5"/>
</LinearLayout>